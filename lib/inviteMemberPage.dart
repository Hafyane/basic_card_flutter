import 'package:flutter/material.dart';

class InviteMemberPage extends StatelessWidget {
  const InviteMemberPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Page Second');
  }
}

